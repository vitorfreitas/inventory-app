import React from 'react'
import { View } from 'react-native'

interface Props {
  t: (path: string) => string
}

const IngredientsContainer: React.SFC<Props> = ({ t }) => <View />

export default IngredientsContainer
