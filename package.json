{
  "private": true,
  "name": "stock",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "app": "yarn workspace app start",
    "server": "yarn workspace @stock/server start",
    "server:dev": "yarn workspace @stock/server start:dev"
  }
}
